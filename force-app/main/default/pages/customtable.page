<apex:page controller="customcntrltable">
  <apex:pageBlock >
   <apex:pageBlockTable value="{!prjlst1}" var="p">
      <apex:column value="{!p.name}"/>
      <apex:column value="{!p.Status__c}"/>
      <apex:column value="{!p.Active_Status__c}"/>
      <apex:column value="{!p.client__c}"/>
   </apex:pageBlockTable>
  </apex:pageBlock>
</apex:page>